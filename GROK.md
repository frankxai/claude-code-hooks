# claude-code-hooks — Grok Harness

Grok .grok/ seeds + excellence hooks present.

Claude Code hooks tooling.

The local .grok/pretooluse-excellence.json and session hooks will enforce the same high bar used across the fleet.

See root docs + the adapter patterns in claude-code-config.

SIP. God 99.